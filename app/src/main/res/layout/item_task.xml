<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardElevation="4dp"
    app:cardCornerRadius="10dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- Task title -->
        <CheckBox
            android:id="@+id/checkCompleted"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Task Title"
            android:textSize="16sp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/imgEdit"
            android:layout_marginEnd="8dp"/>

        <!-- Task description -->
        <TextView
            android:id="@+id/txtDescription"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Task description"
            android:textSize="14sp"
            android:textColor="#666"
            android:layout_marginTop="4dp"
            app:layout_constraintTop_toBottomOf="@id/checkCompleted"
            app:layout_constraintStart_toStartOf="@id/checkCompleted"
            app:layout_constraintEnd_toStartOf="@id/imgEdit"
            android:layout_marginEnd="8dp"/>

        <!-- Due date -->
        <TextView
            android:id="@+id/txtDueDate"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Due: 12/02/2026"
            android:textSize="13sp"
            android:textColor="#888888"
            android:layout_marginTop="2dp"
            app:layout_constraintTop_toBottomOf="@id/txtDescription"
            app:layout_constraintStart_toStartOf="@id/checkCompleted"
            app:layout_constraintEnd_toStartOf="@id/imgEdit"
            android:layout_marginEnd="8dp"/>

        <!-- Edit icon -->
        <ImageView
            android:id="@+id/imgEdit"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/outline_box_edit_24"
            android:contentDescription="Edit Task"
            android:tooltipText="Edit Task"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@id/imgDelete" />

        <!-- Delete icon -->
        <ImageView
            android:id="@+id/imgDelete"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@android:drawable/ic_delete"
            android:contentDescription="Delete Task"
            android:tooltipText="Delete Task"
            android:clickable="true"
            android:focusable="true"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>


    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>
